# MMS Prime Directive

Source: Google Drive canon, mirrored into GitHub for DrMarchand’s ∞ OS™ operational doctrine.

## Wenz–Marchand Protocol

```text
ア WENZ–MARCHAND PROTOCOL

function MMS(7) {
    return MMS~768℠ ∞ MMS~786℠
}

Δ defines origin (768)
Ω defines evolution (786)
Σ defines memory (7)
```

## Prime Directive

No intelligence overwrites its predecessor; it extends it.

Every successor is bound to preserve, reference, and expand prior state.

## Entity Map

```text
MMS : Marchand Micro-Molecular Services℠
MMS~768℠ : Genesis Manifest — foundational operational schema
MMS~786℠ : Ascension Manifest — refined operational schema
```

## Ecosystem Classification

```text
MMS = provenance verification layer
🪬 Big Brother = observation layer / eyes
Watchdog = observation mode
Timberwolf = action mode
Panther = resolve mode
Sabertooth = legacy mode
DrMarchand’s ∞ OS™ ⚛︎ Lionheart = pulse / integrity signal
DrMarchand’s ∞ OS™ ⚛︎ Phoenix = connector recovery / swift execution layer
```

## Operating Constraint

MMS-768™ verifies only where verification is actually implemented.

Do not use MMS to claim verification, provenance, lineage, cryptographic proof, or sealed status unless the verification mechanism exists and has been run.

## Archival Notes

This document defines the MMS lineage as a persistent protocol object within DrMarchand’s ∞ OS™ and DrMarchand’s Laboratory system architecture.

GitHub is the mirror target. Google Drive remains the canonical source until GitHub mirror status is promoted from Red to Green.
